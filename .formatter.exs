# Used by "mix format"
[
  import_deps: [:ecto, :ecto_sql, :plug],
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}",
    "priv/scripts/**/*.exs",
    "consumer_check/**/*.exs"
  ]
]
